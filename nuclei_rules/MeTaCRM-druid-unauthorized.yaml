id: MeTaCRM-druid-unauthorized

info:
  name: MeTaCRM-druid-unauthorized
  author: iSee857
  severity: high
  description: |
    MeTaCRM-druid-unauthorized

http:
  - raw:
      - |
        GET /druid/websession.html HTTP/1.1
        Host: {{Hostname}}

    matchers-condition: and
    matchers:
      - type: word
        part: body
        words:
          - "<title>Druid"
        condition: and

      - type: status
        status:
          - 200